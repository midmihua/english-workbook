import LoginIcon from '../../icons/Login';
import Title from '../Title';

const LoginFormTitle = () => {
  return <Title Icon={LoginIcon} title={`login form`} classes={'title'} />;
};

export default LoginFormTitle;
